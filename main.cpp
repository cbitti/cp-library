#include <bits/stdc++.h>
using namespace std;

using ll  = long long;
using pii = pair<int,int>;

#define all(v) begin(v), end(v)

void fast_io() { ios::sync_with_stdio(false); cin.tie(nullptr); }

void solve() {

}

int main() {
    fast_io();

    int t = 1;
    cin >> t; // Comment this out for single-test problems
    while (t--) solve();

    return 0;
}
